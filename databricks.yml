# This is a Databricks asset bundle definition for dab_project_dec.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project_dec
  uuid: 060edbd3-3a33-4ef9-a656-23cb187e379e

include:
  - resources/*.yml
  - resources/*/*.yml

variables:
  catalog:
    default: citibike_dev

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1230696839458787.7.azuredatabricks.net

    presets:
      # Set dynamic_version: true on all artifacts of type "whl".
      # This makes "bundle deploy" add a timestamp to wheel's version before uploading,
      # new wheel takes over the previous installation even if actual wheel version is unchanged.
      # See https://docs.databricks.com/aws/en/dev-tools/bundles/settings
      artifacts_dynamic_version: true

  test:
    mode: production
    variables:
      catalog:
        default: citibike_test
    presets:
      name_prefix: 'testbaby_'
    workspace:
      host: https://adb-1668767633753449.9.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/dueguduebu_gmail.com#ext#@dueguduebugmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/${bundle.name}/${bundle.target}
    permissions:
      - user_name: dueguduebu_gmail.com#ext#@dueguduebugmail.onmicrosoft.com
        level: CAN_MANAGE

  prod:
    mode: production
    variables:
      catalog:
        default: citibike_prod
    workspace:
      host: https://adb-556792786288124.4.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/dueguduebu_gmail.com#ext#@dueguduebugmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: dueguduebu_gmail.com#ext#@dueguduebugmail.onmicrosoft.com
        level: CAN_MANAGE
